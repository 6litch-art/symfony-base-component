<video {{ html_attributes(attr) }}>
    {% for source in sources %}
    <source src="{{source}}" type="{{ source|mimetype }}">
    {% endfor %}
    {{ "@fields.video.unsupported_tag"|trans }}
</video>