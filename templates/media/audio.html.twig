<audio {{ html_attributes(attr) }}>
    {% for source in sources %}
    <source src="{{source}}" type="{{ source|mimetype }}">
    {% endfor %}
    {{ "@fields.audio.unsupported_element"|trans }}
</audio>
