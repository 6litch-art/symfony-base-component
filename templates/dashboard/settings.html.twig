{% extends "@EasyAdmin/layout.html.twig" %}
{% block content %}

{% if form.children|length > 1 %}
    {{ form(form) }}
{% else %}
    <div class="text-center">
    {{"@dashboard.controllers.settings.empty"|trans}}
    </div>
{% endif %}

{% endblock %}
