
{% set attr = options.attr ?? [] %}
{% set attr = attr|merge({
    'class': 'ql-editor wysiwyg ' ~ (attr.class is defined ? ' ' ~ attr.class : '')
}) %}

<div {{ html_attributes(attr) }}>{{ html|raw }}</div>