<div class="locale-dropdown dropdown">
    <button class="btn dropdown-toggle" type="button" id="dropdown-locale" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="fa fa-globe"></i>
    </button>
    <ul class="dropdown-menu {{ class }}" aria-labelledby="dropdown-locale">
    {% for locale in available_locales %}
        {% set current = (locale == current_locale) ? "active" : null %}
        <li><a href="{{ path(switch_route, {locale: locale}) }}" {{ html_attributes({"class": "dropdown-item "~current}) }}>
            <img class="country-flag" src="{{asset('bundles/base/flags/'~(locale|country)~'.png')}}"> {{ locale|lang_name|capitalize }}
        </a></li>
    {% endfor %}
    </ul>
</div>

