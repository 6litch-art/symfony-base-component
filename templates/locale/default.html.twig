<div class="locale-default">
    <span class="label">{{ "@messages.locale.lang.singular"|trans }}</span>
    <ul class="locale">
    {% for locale in available_locales %}
        {% set current = (locale == current_locale) ? "active" : null %}
        <li><a href="{{ path(switch_route, {locale: locale}) }}" {{ html_attributes({"class":current}) }}>
            <img class="country-flag" src="{{asset('bundles/base/flags/'~(locale|country)~'.png')}}">
        </a></li>
    {% endfor %}
    </ul>
</div>