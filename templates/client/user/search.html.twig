{% extends 'layout.html.twig' %}

{% block title %}Search for a user{% endblock %}
{% block content %}

    {% if user is defined %}
        <div class="mb-3">
            You are logged in as {{ user.username }}, <a href="{{ path('base_logout') }}">Logout</a>
        </div>
    {% endif %}

{% endblock %}
