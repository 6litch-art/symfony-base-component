{% extends 'email.html.twig' %}

{% block content %}

    {{ "notifications.resetPassword.email.content"|trans2([signedUrl|url])|raw }}<br/>

    {% if expiresAtMessageKey is defined and expiresAtMessageKey is not null %}
        {{ "notifications.resetPassword.email.expiracy"|trans2([expiresAtMessageKey])|raw }}
    {% endif %}

{% endblock %}
