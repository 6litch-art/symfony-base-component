{% extends 'layout1.html.twig' %}

{% block content %}
    <div class="content">
        <div class="glass block">

            <main>
                <section class="wrapper tab-content" id="pills-tabContent">
                    <h1>{{ "@controllers.security_pendingForApproval.title"|trans|mb_ucwords }}</h1>
                    <p>{{ "@controllers.security_pendingForApproval.description"|trans([app.user.email])|raw}}</p>
                </section>
            </main>
        </div>
    </div>
{% endblock %}