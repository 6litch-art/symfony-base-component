{% extends 'layout.html.twig' %}
{% block content %}
<div class="page wrapper">

    <h1 class="page-title"></h1>
    <h3></h3>

    {% set referer = app.request.server.get('http-referer')|default(asset('')) %}
    <div class="page-referer">{{ '@controllers.page.return'|trans([referer])|raw }}<p>
</div>
{% endblock %}
