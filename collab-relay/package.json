{
  "name": "base-bundle-collab-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Auth-gated y-websocket relay for glitchr/base-bundle's real-time collaboration feature (EditorType collab_live). Lives inside the bundle so a consuming app's docker-compose.yml can build it via ./vendor/glitchr/base-bundle/collab-relay without a separate repo/registry.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "example:build": "esbuild example/client.js --bundle --outfile=example/dist/bundle.js --format=iife"
  },
  "license": "GPL",
  "dependencies": {
    "ws": "^8.18.0",
    "y-websocket": "^2.1.0",
    "yjs": "^13.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "engines": {
    "node": ">=18"
  }
}
